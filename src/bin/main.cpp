#include "HelloLib.h"

#include "thread.h"
#include <iostream>
#include <iostream>

int main()
{
    RESOURCE_MONITOR::resource_observer observer;
    while (true){
        sleep(1);
    }
    exit(0);
}
